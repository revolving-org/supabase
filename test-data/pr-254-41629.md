# Test PR #41629

Original: https://github.com/supabase/supabase/pull/41629

## Title
chore(design-system): align PageSectionAside to bottom of text content

## State
review

## Body
## What kind of change does this PR introduce?

UI update.

## What is the current behavior?

`PageSectionAside` has its textual content aligned to the vertical center of its sibling `PageSectionSummary`. This makes its buttons look a little float-y when a description is applied.

## What is the new behavior?

- Flex alignment cascading in a way that allows:
	- When `PageSectionDescription` provided: `PageSectionSummary` is aligned to the bottom of `PageSectionAside`
		- `text-box-tr

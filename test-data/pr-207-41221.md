# Test PR #41221

Original: https://github.com/supabase/supabase/pull/41221

## Title
Change flag for API keys last used

## State
merged

## Body
## Context

Swaps out the flag `HideApiKeyLastUsed` to `showApiKeysLastUsed`

As a rule of thumb our feature flags should be assumed to be falsey by default, so that in the event that the flags somehow failed to load due to an upstream provider issue, the UI will then still render as expected.

^ Based on this then we shouldn't need the changes in this PR then: https://github.com/supabase/supabase/pull/41209


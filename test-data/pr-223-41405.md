# Test PR #41405

Original: https://github.com/supabase/supabase/pull/41405

## Title
chore: Migrate `studio` to use `ui-patterns/shimmeringLoader`

## State
merged

## Body
This PR deletes `ShimmeringLoader` from `studio` and migrates it to use `ui-patterns/ShimmeringLoader`.

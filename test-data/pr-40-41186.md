# Test PR #41186

Original: https://github.com/supabase/supabase/pull/41186

## Title
Support creating multiple publishable keys, and deleting publishable keys

## State
merged

## Body
Pending Infra PR: https://github.com/supabase/platform/pull/28246

## Context

Update publishable keys UI to be consistent to that of secret keys. The dashboard implicitly presumes a single publishable key per project, but the API supports multiple, so the changes here are just to support that.
- Support creating multiple publishable keys
- Support deleting publishable keys
- Only show "Create new API keys" CTA if there's no new API keys created (currently checks for if there's at least o

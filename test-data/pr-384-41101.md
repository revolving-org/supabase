# Test PR #41101

Original: https://github.com/supabase/supabase/pull/41101

## Title
chore(flags): migrate homeNew flag from configcat to posthog

## State
merged

## Body
## Summary
Removes all instances of `useFlag('homeNew')` (ConfigCat) and replaces them with `usePHFlag('homeNew')` (PostHog) to consolidate feature flag management for the new home page under a single provider.

## Changes
- Remove `useFlag('homeNew')` from ConfigCat in favor of `usePHFlag('homeNew')` from PostHog
- Simplify dual-flag check in project home page to single PostHog flag check
- Update imports across 3 files to use `usePHFlag` hook

## Testing
**Quick test:**
1. Navigate to `/projec

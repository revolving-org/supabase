# Test PR #41041

Original: https://github.com/supabase/supabase/pull/41041

## Title
studio(fix): remove org redirect

## State
open

## Body
## What kind of change does this PR introduce?

- Remove redirect
- Flow simplification
- Resolves DEPR-318

## What is the current behavior?

It is not possible to view the /organizations page unless you have 1+ organizations (or enter a non-existent org slug). This causes a disorienting redirect when landing on that URL or cancelling org creation.

## What is the new behavior?

- Remove `useEffect` that redirected if `organizations.length <= 0`
- Use already-made `NoOrganizationsS

# Test PR #41080

Original: https://github.com/supabase/supabase/pull/41080

## Title
fix(pg-meta): correct composite foreign key column pairing in tables

## State
open

## Body
## What kind of change does this PR introduce?

bug fix

## What is the current behavior?

the sql query for fetching table relationships in `packages/pg-meta/src/sql/tables.ts` creates incorrect foreign key column pairings for composite foreign keys. this causes the table editor ui to display wrong target tables in tooltips and open wrong table pickers when selecting related records.

for a composite fk like `(user_id, tenant_id) -> (id, tenant_id)`, the buggy query creates a cartesian 

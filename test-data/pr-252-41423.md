# Test PR #41423

Original: https://github.com/supabase/supabase/pull/41423

## Title
Fix table quickstart experiment multi-bucket exposure issue

## State
review

## Body
## Problem

We noticed ~20% of exposures in the table quickstart experiment ended up in multiple buckets. After digging in, turns out this wasn't a PostHog bucketing issue - the flag evaluation is correctly user-level based on gotrue_id.

The actual culprit was our exposure tracking. We were using a React ref (`useRef`) to dedupe the `table_quickstart_opened` event, but refs reset whenever the component remounts. So if someone visited multiple new projects (or just refreshed the page), we'd fire

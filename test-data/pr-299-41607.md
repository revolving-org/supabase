# Test PR #41607

Original: https://github.com/supabase/supabase/pull/41607

## Title
chore: Use `@/*` as an alias for importing in-package files

## State
merged

## Body
This PR sets `@/*` as an alias for importing in-package files in all apps. `~/*` has been deprecated because LLMs can mistake it for home directory and make a mess on the dev machine.

`baseUrl` has been removed (it's deprecated and will be removed in Typescript 7) in `docs`, `design-system` and `ui-library` apps. `studio` and `www` migration will be done in a separate PR because there's a lot of changes.

This is setup PR for prettier sort imports feature.

<!-- This is an auto-generated co

# Test PR #41152

Original: https://github.com/supabase/supabase/pull/41152

## Title
Add columns to database indexes UI

## State
merged

## Body
## Context

For the database indexes page, include a new column "Columns" to show which columns of the table the index applies to

Am also shifting dashboard specific queries out of the `pg-meta` package into dashboard within `data/sql/queries` and assigning postgres team as the codeowners there (based on request from postgres team)

Main changes for this PR are from the files
- `get-indexes.ts`
- `indexes-query.ts`
- `Indexes.ts`

## Before
<img width="1114" height="366" alt="image"

# Test PR #41199

Original: https://github.com/supabase/supabase/pull/41199

## Title
feat(auth): improved user search

## State
merged

## Body
Improved user search is only shown when a user has opted-in and the indexes have successfully been created (see https://github.com/supabase/supabase/pull/40955).

Changes when improved user search is **enabled**:

1. Default to `created_at desc` sorting on page load (uses `idx_users_created_at_desc` index)
2. Remove the `Unified search` option from the sort by dropdown
3. Introduce searching on the `raw_user_metadata.name` column (uses `idx_users_name` index)
4. Simplified generated SQL q

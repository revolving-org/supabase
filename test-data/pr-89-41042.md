# Test PR #41042

Original: https://github.com/supabase/supabase/pull/41042

## Title
Reinstate copy action for TextConfirmModal for staff only

## State
merged

## Body
## Context
Reinstates this PR but only for staff via feature flag 
- https://github.com/supabase/supabase/pull/40765

## Changes involved
- Add a `TextConfirmModalWrapper` that just passes the `enableCopy` prop into `TextConfirmModal` based on the feature flag
- Update all imports to `TextConfirmModal` to use `TextConfirmModalWrapper` instead

## To test
Can use the project delete panel to test
- [x] For a non `supabase` account, verify that there's no copy action in the text confirm m

# Test PR #41636

Original: https://github.com/supabase/supabase/pull/41636

## Title
chore: Bump `next` in the `www` app

## State
review

## Body
This PR bumps the version of `next` in the `www` app. Unfortunately, it's still using the `webpack` build.

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Upgraded Next.js to v16 and aligned related packages; build now runs with explicit webpack and cleanup removes additional cache/artifacts.
  * Switched JSX runtime to the new react-jsx and added server-side support for Octokit packages.

* **Content**
  * Removed cache-bust

# Test PR #41517

Original: https://github.com/supabase/supabase/pull/41517

## Title
Hide RLS disabled CTA in table editor header if no lints

## State
merged

## Body
## Context

Table Editor shows the "RLS Disabled" CTA in the header if the table is as such even if the schema isn't exposed through the Data API. Changes here just adds a check for tables lints (as per how EntityListItem checks for the "unrestricted" badge for consistency) to only show the "RLS Disabled" CTA if the table has lints.

JFYI the lints only considers the `public` schema so if the custom schema is exposed through the API, both "unrestricted" + "RLS Disabled" call outs will not sh

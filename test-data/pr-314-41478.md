# Test PR #41478

Original: https://github.com/supabase/supabase/pull/41478

## Title
fix: set PostHog ui_host for toolbar auth

## State
merged

## Body
**Problem**
Clicking “Authenticate” in the PostHog toolbar on supabase.com was sending us to `ph.supabase.com/.../authorize_and_redirect` and landing on a 404. That blocks us from using the toolbar for debugging/validation (which is basically the whole point of turning it on).

**What happened**
Our `ph.supabase.com` setup is an ingestion proxy (it forwards to `*.i.posthog.com`). The toolbar auth flow isn’t served off the ingestion host though — it lives on the PostHog UI host (`eu.posthog.c

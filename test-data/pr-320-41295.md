# Test PR #41295

Original: https://github.com/supabase/supabase/pull/41295

## Title
chore(studio): delete Confirm Modal component

## State
merged

## Body
## What kind of change does this PR introduce?

- Component defrag
- Resolves DEPR-223
- References DEPR-162

## What is the current behavior?

`ConfirmModal` (also referred to as `ConfirmDialog`) is a deprecated component. We have five other dialog components and the difference between all of them is a bit confusing.

We have a newer `ConfirmationModal` component that does exactly the same job in all instances where `ConfirmModal` is used.

## What is the new behavior?

- `Confirm

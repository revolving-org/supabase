# Test PR #41575

Original: https://github.com/supabase/supabase/pull/41575

## Title
chore(design-system): improve tab focus

## State
review

## Body
## What kind of change does this PR introduce?

UI improvement.

## What is the current behavior?

We either have browser default or inconsistent `focus-visible` states. This makes tabbing through with the keyboard feel quite clunky.

## What is the new behavior?

Consistent `focus-visible` tab states.

## Still to come

- [ ] Better handling of global styles. Is `focus.scss` the best place? Or should it be at the component level? That would keep things agnostic and atomic but isn’

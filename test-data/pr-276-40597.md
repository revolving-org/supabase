# Test PR #40597

Original: https://github.com/supabase/supabase/pull/40597

## Title
chore(studio): improve project upgrade experience

## State
open

## Body
## What kind of change does this PR introduce?

- UI updates
- Resolves DEPR-188

## What is the current behavior?

- Upgrading is ambiguous

## What is the new behavior?

- Brings the upgrade flow out in a review step
- Adds `backingUp` as a first-class status for clarity

## Additional context

The API returns a rounded version number. This makes it ambiguous as to which exact Postgres version a user chocse. We should add a `target_version_display` or `target_version_string` fi

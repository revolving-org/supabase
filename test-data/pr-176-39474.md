# Test PR #39474

Original: https://github.com/supabase/supabase/pull/39474

## Title
chore(design-system): admonition background fill

## State
merged

## Body
## What kind of change does this PR introduce?

UI update and related design documentation. Resolves DEPR-235

## What is the current behavior?

The Admonition component with:
- `variant="warning"`
- `variant="destructive"`
Is barely different to the `default` variant. The only thing that changes is an icon.

Additionally, `Admonition` needlessly added different styles on top of `Alert`.

## What is the new behavior?

- Those two variants get a `warning` fill and border, or `destr

# Test PR #41147

Original: https://github.com/supabase/supabase/pull/41147

## Title
chore(studio): improve alert composition and usage

## State
merged

## Body
## What kind of change does this PR introduce?

Design defrag. Resolves DEPR-227.

## What is the current behavior?

Admonition and Alert components are a bit crufty. Many instance of these clients override these crufty styles as a work-around. Some examples of what I mean:

- The text elements in an Admonition are offset vertically by default. Some instances use `[&>div]:!translate-y-0` or similar.
- The `actions` in an Admonition are often styled with `mt-3` or similar.
- The margins

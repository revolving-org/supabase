# Test PR #41086

Original: https://github.com/supabase/supabase/pull/41086

## Title
fix(studio): update only clicked column when editing fk cell in table editor

## State
open

## Body
## What kind of change does this PR introduce?

bug fix

## What is the current behavior?

when editing a foreign key column in the table editor grid (double-click cell), the ui silently updates all columns in a composite fk constraint. this allows records to move between tenants without user consent, bypassing tenant isolation.

for example, editing `role_id` from `role-1` to `role-2` (which belongs to a different tenant) causes the ui to automatically update `tenant_id` as well, making

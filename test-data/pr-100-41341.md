# Test PR #41341

Original: https://github.com/supabase/supabase/pull/41341

## Title
Fix replica panel closing when clicking new replica CTA from database selector

## State
merged

## Body
## Context

While on the settings -> infrastructure page, if you hit the "Deploy a new replica" button, it opens the replica panel alright. However if you scroll down to "Infrastructure Activity" and click the database selector -> "Create a new read replica" CTA, the replica panel seems to open then close immediately.

Am thinking this is happening due to a shift in browser focus when opening the panel from a "portal" based component (e.g Dialog, Sheet, Dropdown, etc), causing the `onCancel`
